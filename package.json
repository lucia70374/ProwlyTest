{
  "name": "prowlytest",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "npx cucumber-js",
    "generate-report": "node scripts/generate-report.js",
    "test:cucumber": "cucumber-js features/**/*.feature --require features/step_definitions/*.ts --require features/support/*.ts || true && npm run generate-report"
  },
  "keywords": [],
  "author": "Tatjana Vasovic Bursac",
  "license": "ISC",
  "description": "Automated Playwright tests with Gherkin syntax using Cucumber.js and Typescript, with added test scenario/cases.",
  "devDependencies": {
    "@cucumber/cucumber": "^12.2.0",
    "@playwright/test": "^1.55.1",
    "@types/node": "^24.5.2",
    "cucumber-html-reporter": "^6.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "playwright": "^1.55.1"
  }
}
